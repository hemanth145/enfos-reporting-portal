spring.application.name=reporting-portal
server.port=8080

# Serialize java.time types as ISO-8601 strings (e.g. "2026-08-10") rather than arrays.
spring.jackson.serialization.write-dates-as-timestamps=false
